# submissions/ — pending block submissions

Block bundles awaiting review. A contributor submits a block as a GitHub issue
(zip attached) from the **Block upload** page. A maintainer extracts the bundle
here, reviews and tests it, and on acceptance moves it to `../registry/`, where
it is published on the site.
